# 项目进度与接手说明

更新时间：2026年6月15日

## 一、接手结论

项目主体功能已经完成，但尚不能宣布项目全部完成。

新代理无需读取历史聊天，按本文件“立即执行”部分继续即可。当前最重要的工作是：

1. 对最新修复执行一次独立质量复审；
2. 编写 `复核说明.md` 和 `README.md`；
3. 完成最终全项目审查；
4. 将本地尚未推送的提交推送到已存在的公开 GitHub 仓库。

## 二、立即执行

工作目录：

```text
D:\BaiduSyncdisk\claude\equity-incentive-individual-income-tax
```

接手后依次执行：

```powershell
cd D:\BaiduSyncdisk\claude\equity-incentive-individual-income-tax
git status -sb
git log --oneline -12
python -m pytest -q
Get-FileHash -Algorithm SHA256 -LiteralPath '.\股权激励个人所得税计算表-V2-2026.06.11 17点.xlsx'
Get-FileHash -Algorithm SHA256 -LiteralPath '.\广电计量股权激励个人所得税计算及分期台账-2026版.xlsx'
```

本机稳定 Python 路径：

```text
C:\Users\lucheng\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe
```

如果系统 `python` 不可用，执行：

```powershell
& 'C:\Users\lucheng\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m pytest -q
```

预期基线：

```text
76 passed
```

测试约需80至90秒，包含本机 Excel 16.0 COM 打开和重算验证。

## 三、当前版本状态

- 当前分支：`main`
- 工作区：干净
- 公开仓库：[000luc/equity-incentive-individual-income-tax](https://github.com/000luc/equity-incentive-individual-income-tax)
- 仓库可见性：`PUBLIC`
- GitHub登录账号：`000luc`
- 远程：`origin`
- 远程 `main` 当前停在：`3cbd93c`
- 最新功能基线：`0372ad1`
- 其后仅有进度文档提交；本文件提交后，本地预计比远程领先8个提交

接手时重新核对实际差异：

```powershell
git log -1 --oneline main
git log -1 --oneline origin/main
git rev-list --left-right --count origin/main...main
```

推送前必须先完成最终审查、README和复核说明，不要现在直接推送。

## 四、文件边界

### 必须保留且不得修改

原始文件：

```text
股权激励个人所得税计算表-V2-2026.06.11 17点.xlsx
```

原始文件 SHA256：

```text
A320E140E68AF70EAC909E90502C5BC120882D447CFEB141415AA5FE0063E6A0
```

该文件已被 `.gitignore` 排除，不应上传公开仓库。

### 最终交付文件

```text
广电计量股权激励个人所得税计算及分期台账-2026版.xlsx
```

当前本地 SHA256：

```text
5728F13FFDED637A1232069483975E4EB083349F495F89401F728A1BC9B08E17
```

若再次用 Excel 打开并保存，XLSX 元数据可能变化，哈希也会变化。交付时重新计算并更新 `复核说明.md`，不要把旧哈希当固定断言。

### 核心源文件

- `src/tax_model.py`：纯税务计算和分期状态模型。
- `src/build_workbook.py`：生成新版工作簿。
- `tests/test_tax_model.py`：税务模型测试。
- `tests/test_workbook.py`：工作簿结构、公式、COM重算和边界测试。
- `research/公告及政策依据.md`：公告参数和税务政策来源。
- `2026-06-11-equity-incentive-iit-workbook-design.md`：最终设计。
- `docs/superpowers/plans/2026-06-11-equity-incentive-iit-workbook-implementation.md`：实施计划。
- `AGENTS.md`、`CLAUDE.md`：项目执行约束。

## 五、已完成事项

### 1. 公告及政策

已核实：

- 广电计量2023年股票期权与限制性股票激励计划；
- 计划、授予、登记、调整、注销口径；
- 限制性股票登记完成日：2024年8月27日；
- 2024年8月27日收盘价：13.06元；
- 股票期权行权价格调整链：14.56、14.32、14.18、14.04元；
- 股票期权和限制性股票计税公式；
- 同年度累计计税；
- 逐事件新增税额；
- 每次行权或解禁分别起算36个月缴税期限。

### 2. 税务模型

已实现：

- 股票期权所得；
- 限制性股票所得；
- 其他股权激励所得；
- 综合所得年度税率表；
- 同年度累计税额及逐事件新增税额；
- 跨年度分开计税；
- 月末加36个月；
- 超额、逾期、离职和事件批次关联校验；
- `Decimal` 金额计算及按分四舍五入。

### 3. 新版工作簿

包含且仅包含8张业务表：

1. 使用说明
2. 计划参数
3. 激励事件明细
4. 年度计税汇总
5. 分期缴税台账
6. 单人测算
7. 税率及规则
8. 政策及公告

已实现：

- 500行事件录入空间；
- 员工主数据及员工编号验证；
- 公告参数自动引用；
- 同年累计计税、逐事件新增税额和独立截止日；
- 分期台账按事件编号或税额批次统一关联；
- 单人测算按25行分页，覆盖500条数据；
- 重复事件排除出计税；
- 空缴税日期不计入已缴；
- 未来缴款不提前减少余额；
- 离职当天缴款有效，离职后缴款异常；
- 无离职日期不误报；
- 日期验证延伸至2099年；
- 输入、参数、公式和异常颜色区分；
- 数据验证、条件格式、保护、筛选、冻结窗格和打印设置。

### 4. Excel兼容性

已修复过：

- Table和工作表重复 `autoFilter` 导致Excel无法打开；
- `AGGREGATE`、`MINIFS`公式XML兼容问题；
- 年度汇总最早到期事件循环引用；
- 空离职日期返回0导致误报。

本机 Excel 16.0 已成功：

- 打开工作簿；
- 执行全量计算；
- 保存；
- 关闭。

### 5. 自动测试

最近一次实际运行：

```text
76 passed in 82.32s
```

`pytest.ini` 已配置，以下命令应直接可用：

```powershell
python -m pytest -q
pytest -q
```

## 六、最后一次质量审查状态

独立质量审查曾发现：

1. 空缴税日期和未来缴款提前计入已缴；
2. 重复事件虽标异常但仍参与计税；
3. 日期验证上限2035年不足；
4. 缺少稳定的 `pytest` 导入配置。

以上问题已在提交 `0372ad1 fix: enforce payment cutoff and unique events` 中修复，并补充工作簿实算测试。

修复后已验证：

- 正序测试：76项通过；
- 反序测试：76项通过；
- Excel 16.0 COM全量计算成功；
- 原始文件哈希未变化。

但是，修复后的独立质量复审被用户中断，原审查代理已不可恢复。因此该审查闭环仍为“未完成”。

接手代理必须重新分派一个全新、只读的质量审查代理，重点实测：

- 空日期且有缴税金额；
- 未来缴税日期；
- 重复事件编号；
- 2033年至2099年长期日期；
- 分期事件编号与税额批次冲突；
- Excel重算后是否存在 `#REF!`、`#VALUE!`、`#NAME?`、`#DIV/0!`；
- `pytest -q` 正序及反序。

审查通过前不要写“项目已完成”。

## 七、未完成事项

### 1. 独立质量复审

状态：未完成。

要求：

- 使用全新审查代理；
- 只读审查；
- 不相信现有测试报告，实际构造边界数据并用 Excel COM 重算；
- 有问题则由实现代理修复，再复审。

### 2. `复核说明.md`

状态：未创建。

应包含：

- 原表七项主要问题；
- 新版结构和修正；
- 公告及政策依据；
- DEMO001手工复算结果；
- 76项测试结果；
- Excel 16.0实机验证；
- 最终文件哈希；
- 使用限制和维护规则。

### 3. `README.md`

状态：未创建。

公开仓库README应包含：

- 项目用途；
- 工作簿结构；
- 使用流程；
- 计算口径；
- 生成及测试命令；
- 环境依赖；
- 数据隐私边界；
- 原始内部文件不公开；
- 税务政策来源及适用日期；
- 本项目不是自动申报系统。

### 4. 最终全项目审查

状态：未完成。

检查：

- 设计、研究文档、代码、测试和Excel一致；
- 没有真实员工信息；
- 没有内部原始计算表进入Git；
- 公告链接可追溯；
- 工作区干净；
- 全测通过；
- Excel实机打开并全量重算；
- README和复核说明准确。

### 5. GitHub最终发布

状态：仓库已公开，但最新本地提交未推送。

仓库：

```text
https://github.com/000luc/equity-incentive-individual-income-tax
```

最终完成前：

1. 创建并提交 `README.md`、`复核说明.md`；
2. 提交最终审查修复；
3. 确认原始文件未被跟踪；
4. 运行全测和Excel COM验证；
5. 推送 `main`；
6. 用 `gh repo view` 和网页确认仓库公开可访问；
7. 确认远程HEAD等于本地HEAD。

## 八、禁止事项

- 不得覆盖或修改原始Excel。
- 不得将原始Excel加入Git或上传GitHub。
- 不得把公司802万股计划总数作为个人限制性股票计税分母。
- 不得把授予价当成股票登记日收盘价。
- 不得把同年度累计计税误写成所有事件共用同一个36个月截止日。
- 不得只检查公式字符串后声称Excel计算正确。
- 不得在Excel打开失败时盲目重复生成，必须先检查COM错误和XLSX XML。
- 不得在独立质量复审通过前宣布项目完成。

## 九、完成定义

只有同时满足以下条件，项目才算完成：

- 最新独立质量复审为 `Approved`；
- 全量测试全部通过；
- Excel 16.0正常打开、全量计算、保存和关闭；
- 工作簿无公式错误；
- 原始Excel哈希保持不变；
- `复核说明.md` 和 `README.md` 已完成；
- 没有真实员工或内部敏感数据进入公开仓库；
- Git工作区干净；
- 本地最新提交已推送公开GitHub；
- 远程 `main` 与本地 `main` 一致；
- GitHub公开页面可访问。
